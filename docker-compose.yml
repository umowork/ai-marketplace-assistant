version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./bot.db:/app/bot.db

  redis:
    image: redis:7-alpine
    restart: unless-stopped
